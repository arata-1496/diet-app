"use client";
export default function SettingsPage({ store, showToast }: { store: ReturnType<typeof import("@/lib/store").useStore>; showToast: (m: string) => void }) {
  return <div style={{ padding: "60px 18px", textAlign: "center", color: "#8AA0B8" }}>設定画面（準備中）</div>;
}
