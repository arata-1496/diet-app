"use client";
export default function AIPage({ store, showToast }: { store: ReturnType<typeof import("@/lib/store").useStore>; showToast: (m: string) => void }) {
  return <div style={{ padding: "60px 18px", textAlign: "center", color: "#8AA0B8" }}>AI画面（準備中）</div>;
}
